#include "monty.h"
/**
  *f_nop- does nothing but point
  *@head: head of the stack
  *@counter: number of lines
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}

